const addNumber = (number1, number2) => {
    return number1 + number2;
}

const subNumber = (number1, number2) => {
    return number1 - number2;
}

const mulNumber = (number1, number2) => {
    return number1 * number2;
}

// Correct the typo here
module.exports = { addNumber, subNumber, mulNumber };